<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/home.css">
</head>

<body>
    <!-- <%=    %>           <%  %> -->
    <h1>Home Page </h1>
    <section class="feed-posts">
        
        <% if(locals.user) { %>
            <h4><%= user.name  %></h4>
            <form action="/posts/create " id="new-post-form" method="POST">
                <textarea name="content" id="" cols="150" rows="3" placeholder="Post Here..."></textarea>
                <input type="submit" value="Post">
            </form>
            <% } %>
                <div class="posts-container">
                    <p>
                        <h4>Recent Posts</h4>
                        <% for (post of posts ){%>
                            <li>
                                <%= post.content %> <br>
                                    <small><b>-<%= post.user.name %></b></small>
                                    <% if (locals.user && locals.user.id == post.user.id) { %>
                                    <small><a href="/posts/destroy/<%= post.id %>">Delete Post</a></small>
                                    <%} %>
                            </li>
                            <div class="post-comments">
                                <h5>Comments</h5>
                                <div class="post-comments-list">
                                    <ul id="post-comments-<%= post._id  %>">
                                     <p>   <%for (comment of post.comments){%>
                                            <%=comment.content    %> <br>
                                            <small>-<%= comment.user.name %></small><br>
                                      <% } %>  </p>  </ul>
                                </div>
                                <% if(locals.user) { %>
                                    <form action="/comments/create" method="POST">
                                        <textarea type name="content" id="" cols="25" rows="1"
                                            placeholder="Comment Here"></textarea>
                                        <input type="hidden" name="post" value="<%= post._id %>">
                                        <input type="submit" value="Comment">
                                    </form>
                                    <% } %>
                            </div>

                            <% } %>
                    </p>
                </div>
    </section>

</body>

</html>